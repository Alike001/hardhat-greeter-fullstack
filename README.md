# hardhat-greeter-fullstack

A learning project: a Greeter smart contract written in Solidity, a React frontend that reads and writes to it, and the whole thing dockerized so you can spin it up with one command.
